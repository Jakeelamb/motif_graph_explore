// Global default params
params {
    input = "accensions.txt"
    outdir = "./results"
    publish_dir_mode = "copy"
    temp_dir = "./temp"    // Directory for temporary files
    skip_softmasking = true  // Skip softmasking step if set to true
    
    // SLURM cluster parameters - all optional
    slurm_account = "null"  // Optional SLURM account
    slurm_partition = "day-long-cpu"  // Default partition (if not specified by process)
    slurm_qos = "null"        // Default QOS (if not specified by process)
    
    // Process-specific partition settings
    fetch_genome_partition = "short-cpu"
    index_genome_partition = "day-long-cpu"
    softmask_genome_partition = "day-long-cpu"
    windows_partition = "short-cpu"
    streme_partition = "week-long-highmem"
    trf_partition = "week-long-highmem"
    append_db_partition = "short-cpu"
    clean_temp_partition = "short-cpu"
    
    // Process-specific QOS settings (with null defaults)
    fetch_genome_qos = null
    index_genome_qos = null
    softmask_genome_qos = null
    windows_qos = null
    streme_qos = null
    trf_qos = null
    append_db_qos = null
    clean_temp_qos = null
    
    // Conda settings - paths for different environments
    conda_hpc_path = "/nfs/home/jlamb/bin/miniconda3"
    conda_local_path = "/home/jake/miniconda3"  // Correct path without bin directory
    
    // Automatically detect which conda path to use
    conda_path = {
        def hpcPath = new File(params.conda_hpc_path)
        def localPath = new File(params.conda_local_path)
        
        if (hpcPath.exists()) {
            return params.conda_hpc_path
        } else if (localPath.exists()) {
            return params.conda_local_path
        } else {
            // Fallback to use environment detection
            return System.getProperty("user.home").contains("jlamb") ? 
                   params.conda_hpc_path : params.conda_local_path
        }
    }
    
    // Derived path to the conda environment
    conda_env_path = "${params.conda_path}/envs/motif"
    
    // Path to conda activation script
    conda_activate_script = "${params.conda_path}/etc/profile.d/conda.sh"
}

// Define a custom function to check for system type - can be used in process scripts
def getCondaInitCommand() {
    """
    # Detect and use the appropriate conda initialization
    if [ -f "${params.conda_hpc_path}/etc/profile.d/conda.sh" ]; then
        source "${params.conda_hpc_path}/etc/profile.d/conda.sh"
        echo "[INFO] Using HPC conda path: ${params.conda_hpc_path}"
    elif [ -f "${params.conda_local_path}/etc/profile.d/conda.sh" ]; then
        source "${params.conda_local_path}/etc/profile.d/conda.sh"
        echo "[INFO] Using local conda path: ${params.conda_local_path}"
    else
        echo "[ERROR] Could not find conda activation script in either location"
        exit 1
    fi
    
    # Activate the motif environment
    conda activate motif
    """
}

// Default process configurations - applied unless overridden by profiles or withName
process {
    // Use the existing conda environment instead of creating from environment.yml
    conda = params.conda_env_path
    
    // Add the conda initialization script to all processes
    beforeScript = getCondaInitCommand()
}

profiles {
    standard {
        process.executor = 'local'
        // Set default resource limits for local execution
        process.cpus = 16
        process.memory = '15 GB'
        // Optional: Set a default time limit if needed, e.g., time = '24h'
    }
    slurm {
        process.executor = 'slurm'
        // Default SLURM cluster options - can be overridden by process-specific settings
        process.clusterOptions = {
            def options = []

            if (params.slurm_account) {
                options.add("--account=${params.slurm_account}")
            }

            if (params.slurm_partition) {
                options.add("--partition=${params.slurm_partition}")
            }

            if (params.slurm_qos) {
                options.add("--qos=${params.slurm_qos}")
            }

            return options.join(' ')
        }

        // Process-specific resource configurations for SLURM
        withName: fetch_genome {
            cpus = 2
            memory = '4 GB'
            time = '1h'

            clusterOptions = {
                def options = []

                if (params.slurm_account) {
                    options.add("--account=${params.slurm_account}")
                }

                options.add("--partition=${params.fetch_genome_partition}")

                def qos = params.fetch_genome_qos ?: params.slurm_qos
                if (qos) {
                    options.add("--qos=${qos}")
                }

                return options.join(' ')
            }
        }
        
        withName: index_genome {
            cpus = 2
            memory = '8 GB'
            time = '1h'
            
            clusterOptions = { 
                def options = []
                
                if (params.slurm_account) {
                    options.add("--account=${params.slurm_account}")
                }
                
                options.add("--partition=${params.index_genome_partition}")
                
                def qos = params.index_genome_qos ?: params.slurm_qos
                if (qos) {
                    options.add("--qos=${qos}")
                }
                
                return options.join(' ')
            }
        }
        
        withName: softmask_genome {
            cpus = 16
            memory = '32 GB'
            time = '24h'
            
            clusterOptions = { 
                def options = []
                
                if (params.slurm_account) {
                    options.add("--account=${params.slurm_account}")
                }
                
                options.add("--partition=${params.softmask_genome_partition}")
                
                def qos = params.softmask_genome_qos ?: params.slurm_qos
                if (qos) {
                    options.add("--qos=${qos}")
                }
                
                return options.join(' ')
            }
        }
        
        withName: make_extract_windows {
            cpus = 4
            memory = '16 GB'
            time = '2h'
            
            clusterOptions = { 
                def options = []
                
                if (params.slurm_account) {
                    options.add("--account=${params.slurm_account}")
                }
                
                options.add("--partition=${params.windows_partition}")
                
                def qos = params.windows_qos ?: params.slurm_qos
                if (qos) {
                    options.add("--qos=${qos}")
                }
                
                return options.join(' ')
            }
        }
        
        withName: STREME {
            cpus = 24
            memory = '64 GB'
            time = '168h'
            
            clusterOptions = { 
                def options = []
                
                if (params.slurm_account) {
                    options.add("--account=${params.slurm_account}")
                }
                
                options.add("--partition=${params.streme_partition}")
                
                def qos = params.streme_qos ?: params.slurm_qos
                if (qos) {
                    options.add("--qos=${qos}")
                }
                
                return options.join(' ')
            }
        }
        
        withName: TRF {
            cpus = 4
            memory = '32 GB'
            time = '24h'
            
            clusterOptions = { 
                def options = []
                
                if (params.slurm_account) {
                    options.add("--account=${params.slurm_account}")
                }
                
                options.add("--partition=${params.trf_partition}")
                
                def qos = params.trf_qos ?: params.slurm_qos
                if (qos) {
                    options.add("--qos=${qos}")
                }
                
                return options.join(' ')
            }
        }
        
        withName: append_to_db {
            cpus = 2
            memory = '8 GB'
            time = '1h'
            
            clusterOptions = { 
                def options = []
                
                if (params.slurm_account) {
                    options.add("--account=${params.slurm_account}")
                } 
                
                options.add("--partition=${params.append_db_partition}")
                
                def qos = params.append_db_qos ?: params.slurm_qos
                if (qos) {
                    options.add("--qos=${qos}")
                }
                
                return options.join(' ')
            }
        }
        
        withName: clean_temp {
            cpus = 1
            memory = '2 GB'
            time = '1h'
            
            clusterOptions = { 
                def options = []
                
                if (params.slurm_account) {
                    options.add("--account=${params.slurm_account}")
                }
                
                options.add("--partition=${params.clean_temp_partition}")
                
                def qos = params.clean_temp_qos ?: params.slurm_qos
                if (qos) {
                    options.add("--qos=${qos}")
                }
                
                return options.join(' ')
            }
        }
    }
    
    // Special profile for memory-intensive tasks
    highmem {
        process.executor = 'slurm'
        
        withName: STREME {
            cpus = 24
            memory = '128 GB'
            time = '168h'
            
            clusterOptions = {
                def options = []
                
                if (params.slurm_account) {
                    options.add("--account=${params.slurm_account}")
                }
                
                options.add("--partition=week-long-highmem")
                
                return options.join(' ')
            }
        }
        
        withName: softmask_genome {
            cpus = 16
            memory = '128 GB' 
            time = '24h'
            
            clusterOptions = {
                def options = []
                
                if (params.slurm_account) {
                    options.add("--account=${params.slurm_account}")
                }
                
                options.add("--partition=day-long-highmem")
                
                return options.join(' ')
            }
        }
    }
    
    // Special profile for local execution
    local {
        process.executor = 'local'
        process.cpus = 8
        process.memory = '8 GB'
    }
}

// Enable conda
conda {
    enabled = true
    useMamba = false
    // Don't create new environments, use the existing one
    createEnvironments = false
    cacheDir = "${params.conda_path}/envs"
}

// Manifest for pipeline information
manifest {
    name = 'motif_graph_explore'
    author = 'jakeelamb'
    description = 'denovo motif graph generator and assembly index calculator'
    version = '1.0.0'
    nextflowVersion = '>=21.10.0'
}

// Reporting
timeline {
    enabled = true
    file = "${params.outdir}/pipeline_info/execution_timeline.html"
    overwrite = true
}
report {
    enabled = true
    file = "${params.outdir}/pipeline_info/execution_report.html"
    overwrite = true
} 
