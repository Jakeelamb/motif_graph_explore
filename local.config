params {
    // Add a parameter to skip softmasking
    skip_softmasking = true
}

// Create a mock process for softmasking to pass through the data
process {
    withName: 'softmask_genome' {
        enabled = !params.skip_softmasking
    }
} 